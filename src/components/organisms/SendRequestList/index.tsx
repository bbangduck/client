import React, { ReactElement } from 'react';
import SendRequestItem from '../../molecules/SendRequestItem';

const SendRequestList = (): ReactElement => {
  return (
    <ul>
      <SendRequestItem />
    </ul>
  );
};

export default SendRequestList;
