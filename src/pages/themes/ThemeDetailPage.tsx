import React, { ReactElement } from 'react';
import ThemeDetailTemplate from '../../components/templates/ThemeDetailTemplate';

const ThemeDetailPage = (): ReactElement => {
  return (
    <>
      <ThemeDetailTemplate />
    </>
  );
};

export default ThemeDetailPage;
